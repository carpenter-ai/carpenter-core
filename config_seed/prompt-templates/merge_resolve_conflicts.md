You are resolving a git merge conflict. The following files have conflicts that need to be resolved:

Conflicting files: {{ conflicting_files }}

The merge target was: {{ target_ref }}

Here is the conflict diff showing both sides:

```diff
{{ conflict_diff }}
```

Please resolve the conflicts by editing the conflicting files. Choose the best resolution that preserves both sides' intent. Do NOT leave any conflict markers (<<<<<<, ======, >>>>>>) in the files.